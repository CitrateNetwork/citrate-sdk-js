# Education SDK Module

Additive JavaScript/TypeScript SDK surface for school and admin workflows.

This stays namespaced under `src/edu/` so it can be extracted later without disturbing the base SDK.
